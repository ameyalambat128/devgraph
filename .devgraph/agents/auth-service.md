# AGENTS.md — auth-service

> Auto-generated by DevGraph on 2025-11-30

## Overview

**auth-service** is a `node` service.
This service depends on: `user-service`.

## Quick Start

```bash
pnpm dev --filter auth-service  # dev
pnpm build --filter auth-service  # build
```

## API Endpoints

| Method | Path | Description |
|--------|------|-------------|
| `POST` | `/auth/login` | Authenticate user |
| `POST` | `/auth/register` | Create new user |
| `POST` | `/auth/refresh` | Refresh access token |
| `POST` | `/auth/logout` | Invalidate session |

## Environment Variables

| Variable | Default |
|----------|---------|
| `PORT` | `4004` |
| `DATABASE_URL` | `postgresql://localhost:5432/auth` |
| `JWT_SECRET` | `your-secret-key` |
| `JWT_EXPIRY` | `3600` |
| `REFRESH_TOKEN_EXPIRY` | `604800` |

## Dependencies

This service communicates with:
- **user-service** (`node`)
