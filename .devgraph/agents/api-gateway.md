# AGENTS.md — api-gateway

> Auto-generated by DevGraph on 2025-11-30

## Overview

**api-gateway** is a `node` service.
This service depends on: `order-service`, `product-service`, `user-service`.

## Quick Start

```bash
pnpm dev --filter api-gateway  # dev
pnpm build --filter api-gateway  # build
```

## API Endpoints

| Method | Path | Description |
|--------|------|-------------|
| `GET` | `/health` | Health check endpoint |
| `GET` | `/api/products` | List all products |
| `GET` | `/api/products/:id` | Get product by ID |
| `POST` | `/api/cart` | Add item to cart |
| `GET` | `/api/cart` | Get current cart |
| `POST` | `/api/orders` | Create new order |
| `GET` | `/api/orders/:id` | Get order by ID |
| `POST` | `/api/auth/login` | User login |
| `POST` | `/api/auth/register` | User registration |
| `POST` | `/api/auth/refresh` | Refresh JWT token |

## Environment Variables

| Variable | Default |
|----------|---------|
| `PORT` | `4000` |
| `ORDER_SERVICE_URL` | `http://localhost:4001` |
| `PRODUCT_SERVICE_URL` | `http://localhost:4002` |
| `USER_SERVICE_URL` | `http://localhost:4003` |
| `AUTH_SERVICE_URL` | `http://localhost:4004` |
| `REDIS_URL` | `redis://localhost:6379` |
| `JWT_SECRET` | `your-secret-key` |

## Dependencies

This service communicates with:
- **order-service** (`node`)
- **product-service** (`node`)
- **user-service** (`node`)
