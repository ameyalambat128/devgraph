# AGENTS.md — user-service

> Auto-generated by DevGraph on 2025-11-30

## Overview

**user-service** is a `node` service.

## Quick Start

```bash
pnpm dev --filter user-service  # dev
pnpm build --filter user-service  # build
```

## Environment Variables

| Variable | Default |
|----------|---------|
| `PORT` | `4003` |
| `DATABASE_URL` | `postgresql://localhost:5432/users` |
