# AGENTS.md — order-service

> Auto-generated by DevGraph on 2025-11-30

## Overview

**order-service** is a `node` service.
This service depends on: `product-service`, `payment-service`.

## Quick Start

```bash
pnpm dev --filter order-service  # dev
pnpm build --filter order-service  # build
```

## API Endpoints

| Method | Path | Description |
|--------|------|-------------|
| `POST` | `/orders` | Create order |
| `GET` | `/orders/:id` | Get order by ID |
| `PATCH` | `/orders/:id/status` | Update order status |
| `GET` | `/orders/user/:userId` | Get orders by user |

## Environment Variables

| Variable | Default |
|----------|---------|
| `PORT` | `4001` |
| `DATABASE_URL` | `postgresql://localhost:5432/orders` |
| `RABBITMQ_URL` | `amqp://localhost:5672` |
| `PAYMENT_SERVICE_URL` | `http://localhost:4005` |

## Dependencies

This service communicates with:
- **product-service** (`node`)
- **payment-service** (`node`)
